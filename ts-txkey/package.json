{
  "name": "ts-txkey",
  "version": "1.0.2",
  "description": "TypeScript wrapper for txkey binary that generates unique short keys for k/v stores",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:cov": "jest --coverage",
    "example": "ts-node src/example.ts",
    "dev": "ts-node src/index.ts"
  },
  "keywords": ["txkey", "key-generation", "redis", "valkey"],
  "author": "dpw",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
