{
  "name": "valkey-spaces-database",
  "version": "1.0.0",
  "description": "Hybrid database combining Valkey caching with S3 persistence",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "npx dotenvx run -- nodemon",
    "start": "npx dotenvx run -- node dist/index.js",
    "test": "jest",
    "test:unit": "jest tests/unit",
    "test:integration": "jest tests/integration",
    "test:watch": "jest --watch",
    "test:cov": "jest --coverage",
    "test:unit-only": "SKIP_INTEGRATION_TESTS=true SKIP_CLI_TESTS=true jest",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "format": "prettier --write src/**/*.ts",
    "format:check": "prettier --check src/**/*.ts",
    "create-user": "npx dotenvx run -- node dist/cli/index.js create-user",
    "find-user": "npx dotenvx run -- node dist/cli/index.js find-user",
    "get-user": "npx dotenvx run -- node dist/cli/index.js get-user",
    "update-user": "npx dotenvx run -- node dist/cli/index.js update-user",
    "list-users": "npx dotenvx run -- node dist/cli/index.js list-users",
    "get-user-count": "npx dotenvx run -- node dist/cli/index.js get-user-count",
    "verify-email-index": "npx dotenvx run -- node dist/cli/index.js verify-email-index",
    "rebuild-email-index": "npx dotenvx run -- node dist/cli/index.js rebuild-email-index",
    "sync-email-index": "npx dotenvx run -- node dist/cli/index.js sync-email-index",
    "health-check": "npx dotenvx run -- node dist/cli/index.js health-check"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "@dotenvx/dotenvx": "^1.48.3",
    "date-fns": "^2.30.0",
    "iovalkey": "^0.3.3",
    "winston": "^3.10.0",
    "winston-daily-rotate-file": "^4.7.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "eslint": "^8.50.0",
    "jest": "^29.0.0",
    "nodemon": "^3.0.0",
    "pm2": "^5.3.0",
    "prettier": "^3.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  },
  "keywords": [
    "valkey",
    "redis",
    "s3",
    "database",
    "typescript",
    "cli"
  ],
  "author": "dpw",
  "license": "MIT"
}
